<!DOCTYPE html>
<html lang="zh-TW">
  <head>
    <%- include ('templates/head') %>
  </head>
  <script>
    function del() {
      var msg = "您真的確定要歸還嗎？\n\n按確認後即歸還！";
      if ( confirm(msg)==true )
      {
        return true;
      }
      else
      {
        return false;
      }
      }
  </script>
  <body>
    <%- include ('templates/nav') %>
    <div class="manage">
      <!-- <h1>管理後台</h1> -->
      
      <!-- <p class="explain">歡迎使用管理介面~</p> -->
      
      <div class="manage">

        <h1>登記循環杯歸還</h1>
  
        <form method="POST" class="manage__edit" action="/user">
          <div class="content">
            <h2>請掃描或輸入循環杯編號（可連續掃描）</h2>
            <input  placeholder="請點擊此欄位開始掃描 （手動輸入完需按 ENTER 鍵）" type="text" name="orderId"/>
          </div>
          <div class="remind">
            <% if (errorMessage[0]) { %> <%= errorMessage %> <% } %>
          </div>
          <div class="post__btn" hidden>
            <button type="submit" class="edit-post__btn">送出</button>
          </div>
        </form>
        <!-- <% if (tempList[0]) { %> 
          <% for(var i = 0; i < tempList.length; i++) { %>
            <p><%= tempList[i] %></p>
          <% } %>
          <% } %> -->
        <!-- <iframe name="main" src="" id="main" frameborder="0" width="100%" height="1000px" scrolling="auto"></iframe>
         -->
      </div>
      <br>
      <div class="manage__title">
        <a href="/user/cancel">取消所有歸還</a>
        <a href="/user/result" onclick="javascript:return del()">點擊送出歸還</a>
        <br>
        <h3>目前掃描的編號</h3>
        <% for(var i = 0; i < tempList.length; i++) { %>
          <p><%= tempList[i] %></p>
        <% } %>
      </div>
    <%- include ('templates/footer') %>
  </body>
</html>